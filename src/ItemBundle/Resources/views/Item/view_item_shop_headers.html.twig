<thead>
    <tr>
        <th class="title text-left" colspan="{{ item.fieldsCount + 1 }}">
        {{ key|capitalize|trans }}
        </th>
    </tr>
    <tr>
        <th>
            {{ 'admin.item.name'|trans }}
        </th>
        <th>
            {{ 'admin.item.price'|trans }}
        </th>
        {% include item.shopViewHeadersFragment %}
        <th class="text-left">
            {{ 'admin.item.description'|trans }}
        </th>
        <th>
            {{ 'form.label.qte'|trans }}
        </th>
    </tr>
</thead>
